---
import Layout from '../components/Layout.astro';
import { config } from '../config';

const base = import.meta.env.BASE_URL;
---

<Layout title="About - Volks-Typo" description="Learn more about the author and the story behind Volks-Typo" showSidebar={true}>
  <div class="about-page">
    <header class="page-header">
      <h1 class="page-title">About</h1>
      <hr class="page-rule" />
    </header>

    <div class="about-content">
      <div class="author-section">
        <div class="author-image">
          {config.author.avatar ? (
            <img src={config.author.avatar} alt={config.author.name} class="author-photo" />
          ) : (
            <div class="author-placeholder">
              <svg width="80" height="80" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                <circle cx="12" cy="7" r="4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </div>
          )}
        </div>

        <div class="author-info">
          <h2 class="author-name">{config.author.name}</h2>
          <p class="author-bio">{config.author.bio}</p>
        </div>
      </div>

      <div class="about-text">
        <p>
          Welcome to <strong>Volks-Typo</strong>, a design exploration that sits at the intersection of 
          historical typography and contemporary digital expression. This blog examines the tension 
          between clean modernist principles and the weight of institutional design language.
        </p>

        <p>
          Through careful analysis of typography, visual hierarchy, and design systems, I explore 
          how historical aesthetics can inform and enhance modern digital experiences. Each post 
          investigates the delicate balance between functionality and visual impact.
        </p>

        <p>
          The theme itself embodies this philosophy—combining Bauhaus-inspired grid systems with 
          institutional typography, creating what I call "dissonant harmony." It's design that 
          speaks to both the rational and the emotional, the structured and the expressive.
        </p>
      </div>

      <div class="social-section">
        <h3 class="social-title">Connect</h3>
        <div class="social-links">
          {config.social.github && (
            <a href={config.social.github} class="social-link" target="_blank" rel="noopener noreferrer">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>GitHub</span>
            </a>
          )}

          {config.social.linkedin && (
            <a href={config.social.linkedin} class="social-link" target="_blank" rel="noopener noreferrer">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                <rect x="2" y="9" width="4" height="12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                <circle cx="4" cy="4" r="2" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>LinkedIn</span>
            </a>
          )}

          {config.social.twitter && (
            <a href={config.social.twitter} class="social-link" target="_blank" rel="noopener noreferrer">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M23 3a10.9 10.9 0 0 1-3.14 1.53 4.48 4.48 0 0 0-7.86 3v1A10.66 10.66 0 0 1 3 4s-4 9 5 13a11.64 11.64 0 0 1-7 2c9 5 20 0 20-11.5a4.5 4.5 0 0 0-.08-.83A7.72 7.72 0 0 0 23 3z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>X (Twitter)</span>
            </a>
          )}

          {config.social.email && (
            <a href={`mailto:${config.social.email}`} class="social-link">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                <polyline points="22,6 12,13 2,6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>Email</span>
            </a>
          )}
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  .about-page {
    max-width: 800px;
    margin: 0 auto;
  }

  .page-header {
    text-align: center;
    margin-bottom: calc(var(--grid-unit) * 8);
  }

  .page-title {
    font-family: var(--font-heading-primary);
    font-size: 3.5rem;
    font-weight: 900;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    line-height: 1.1;
    margin-bottom: calc(var(--grid-unit) * 4);
  }

  .page-rule {
    height: 4px;
    background-color: var(--color-accent);
    border: none;
    width: 120px;
    margin: 0 auto;
  }

  .about-content {
    display: flex;
    flex-direction: column;
    gap: calc(var(--grid-unit) * 6);
  }

  .author-section {
    display: flex;
    gap: calc(var(--grid-unit) * 4);
    align-items: flex-start;
    padding: calc(var(--grid-unit) * 4);
    background-color: var(--color-surface);
    border-radius: 8px;
    border: 1px solid var(--color-border);
  }

  .author-image {
    flex-shrink: 0;
  }

  .author-photo {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    border: 3px solid var(--color-border);
    object-fit: cover;
  }

  .author-placeholder {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    border: 3px solid var(--color-border);
    background-color: var(--color-light-gray);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-medium-gray);
  }

  .author-info {
    flex: 1;
    min-width: 0;
  }

  .author-name {
    font-family: var(--font-heading-secondary);
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: calc(var(--grid-unit) * 1.5);
  }

  .author-bio {
    font-size: 1.125rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    margin: 0;
  }

  .about-text {
    display: flex;
    flex-direction: column;
    gap: calc(var(--grid-unit) * 3);
  }

  .about-text p {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--color-text-primary);
    margin: 0;
  }

  .about-text strong {
    font-weight: 600;
    color: var(--color-accent);
  }

  .social-section {
    padding: calc(var(--grid-unit) * 4);
    background-color: var(--color-surface);
    border-radius: 8px;
    border: 1px solid var(--color-border);
  }

  .social-title {
    font-family: var(--font-heading-secondary);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: calc(var(--grid-unit) * 3);
    text-align: center;
  }

  .social-links {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: calc(var(--grid-unit) * 2);
  }

  .social-link {
    display: flex;
    align-items: center;
    gap: calc(var(--grid-unit));
    padding: calc(var(--grid-unit) * 1.5);
    background-color: var(--color-background);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    color: var(--color-text-primary);
    text-decoration: none;
    font-family: var(--font-heading-secondary);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-size: 0.9rem;
    transition: all 0.2s ease;
  }

  .social-link:hover,
  .social-link:focus {
    background-color: var(--color-accent);
    border-color: var(--color-accent);
    color: white;
    text-decoration: none;
    transform: translateY(-2px);
  }

  .social-link svg {
    flex-shrink: 0;
  }

  @media (max-width: 768px) {
    .page-title {
      font-size: 2.5rem;
    }

    .author-section {
      flex-direction: column;
      text-align: center;
      padding: calc(var(--grid-unit) * 3);
    }

    .author-photo,
    .author-placeholder {
      width: 100px;
      height: 100px;
      margin: 0 auto;
    }

    .author-name {
      font-size: 1.5rem;
    }

    .about-text p {
      font-size: 1rem;
    }

    .social-links {
      grid-template-columns: 1fr;
    }
  }
</style>